services:
  nlw-agents-pg:
    image: pgvector/pgvector:pg17
    environment:
      POSTGES_USER: docer
      POSTGES_PASSWORD: docker
      POSTGES_DB: agents
    ports:
      - "5432:5432"
    volumes:
      - ./docker/setup.sql:/docker-entrypoint-initdb.d/setup.sql